import React from 'react';
import { StyleSheet, View, FlatList, Text } from 'react-native';

const SerieFormPage = props => (
	<View>
		<Text>Formulário de Série</Text>
	</View>
);

const styles = StyleSheet.create({
	
});

export default SerieFormPage;